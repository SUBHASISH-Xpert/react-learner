in terminal install - npm i react-router-dom
router-dom basicaly one page to another page like about click about page , contack click contact page , login click login page

router -> 1. Browser Router - i use it 
          2. HashRouter
          3. MemoryRouter  - memory ma store
          4. Stitic router
          5. History router 

in main.js -> import { BrowserRouter } from 'react-router-dom' and <BrowserRoter>
    <App />
<BrowserRoter />

next.js -> static routing 
react Native -> memory routing

app.js ->
import { Routes, Route } from 'react-router-dom'

1. Basic routes
2. nested routes
3. dynamic routes
4. usenavigate
5. 404 page
